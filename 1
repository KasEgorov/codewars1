function array(arr1, arr2) {
  
     arr1 = arr1.reduce(function(sum, elem){
             return sum + elem;
         },0);

           arr2 =arr2.reduce(function(sum, elem){
            return sum + elem;
        },0);
        console.log(arr1 + arr2);
}
array([1,2,3],[1,2,3]);
